import { Request, Response } from "express";
import mongoose from "mongoose";
import RFP from "../model/RFP";

const createRfp = async(req: Request, res: Response)=>{
    const rfp = new RFP({
        
    })
}