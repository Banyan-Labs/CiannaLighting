export enum ActionType {
    ADD = 1,
    REPLACE,
    DELETE
}

export enum CopyType {
    PROJECT = 1,
    ROOM
}

export enum AttachmentType {
    IMAGE = 'images',
    PDF = 'pdf',
    SPEC = 'specs',
    DRAWING_FILE = 'drawingFiles',
}
